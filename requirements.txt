# --- LLM w chmurze (Claude / Anthropic) ---
anthropic>=0.40

# --- Transkrypcja (Whisper) + embeddingi w chmurze (OpenAI) ---
openai>=1.40

# --- Wczytywanie kluczy API z pliku .env ---
python-dotenv>=1.0

# --- Audio (przechwytywanie mikrofonu lokalnie) ---
sounddevice>=0.4.6         # potrzebuje biblioteki systemowej PortAudio
numpy>=1.24

# --- RAG / baza wiedzy ---
langchain-community>=0.2.0  # FAISS + loadery dokumentów
langchain-openai>=0.1.7     # embeddingi OpenAI dla langchain
langchain-text-splitters>=0.2.0
faiss-cpu>=1.8.0
pypdf>=4.0                  # ładowanie plików .pdf

# --- Globalny skrót klawiszowy ---
keyboard>=0.13.5           # na Linux wymaga uprawnień root

# --- GUI (pływające okno, always-on-top) ---
PyQt6>=6.6
